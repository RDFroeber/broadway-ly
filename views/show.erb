<h2><%= @a_show.title %></h2>

<div class="show">
   <%= @a_show.title %>
   <%= @a_show.year %>
   <%= @a_show.composer  %>
   <img src="<%= @a_show.img_url %>" alt="<%= @a_show.title %> poster" />
</div>

<a href="/shows/<%= @a_show.id %>/songs/new">Add a new song to this show</a>