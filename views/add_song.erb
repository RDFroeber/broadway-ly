<h2>Add a New Song</h2>
                                       <!--FIX PATH -->
<form action="/shows/<%= :id%>/songs" method="POST">
   <p>Song title:
      <input type="text" name="title" placeholder="Song title" /></p>
   <p>YouTube url:
      <input type="text" name="embed_url" placeholder="http://www.youtube.com/" /></p>
   <p>What show is this song from?
      <input type="text" name="show_id" placeholder="Show title" /></p>
      <button>Add Song</button>
</form>
